# Pinned 2026-07-10 (was all >=): deploys build natively on the VM, so
# unpinned ranges pulled whatever shipped upstream that morning. Bump
# deliberately: edit the pin, let CI run the suite, deploy.
fastapi==0.139.0
uvicorn[standard]==0.51.0
websockets==16.1
sqlalchemy==2.0.51
publicdotcom-py==0.1.18
ib_async==2.1.0
discord.py==2.7.1
python-dotenv==1.2.2
PyJWT[crypto]==2.13.0
httpx==0.28.1
aiohttp==3.14.3
boto3==1.43.46
docker==7.2.0
google-genai==2.11.0
# /api/export/all only. Pure Python, no compiled extension, not on the order
# path — a failure here cannot affect trading.
openpyxl==3.1.5
